# --- Rhumi website ---
# This site is a STATIC export (deployed to GitHub Pages at mubinn.com/rhumi),
# so there is no server and no runtime environment to configure.
#
# The beta waitlist posts directly to a Google Form. Configure it in code, not
# here: edit `lib/waitlist.ts` (formAction + emailEntry) — see the setup notes
# in that file and in the README.
